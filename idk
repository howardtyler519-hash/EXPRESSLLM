let el = document.querySelector('.sn-nav');
while(el) {
  const s = getComputedStyle(el);
  if(s.overflow !== 'visible' || s.overflowY !== 'visible') 
    console.log(el.tagName, el.className, 'overflow:', s.overflow, s.overflowY);
  el = el.parentElement;
}
